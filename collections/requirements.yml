---
# Ansible Collection Requirements for AIOps Project
# These collections are installed during AAP project sync.
# Note: ansible.utils is auto-installed as a dependency of ansible.mcp.

collections:
  # Model Context Protocol Client (Required for MCP integration)
  - name: ansible.mcp
    # source: https://aapaio.lab.gineesh.com/api/galaxy/content/rh-certified/

  # Ansible Utilities (Dependency of ansible.mcp, in 'published' repo)
  - name: ansible.utils

  # SCM Git Operations (clone, branch, commit, push)
  - name: ansible.scm
    # source: https://aapaio.lab.gineesh.com/api/galaxy/content/published/

  # Event-Driven Automation (Required for rulebooks)
  # - name: ansible.eda
  #   version: ">=1.0.0"

  # AAP Controller (Required for MCP and configuration)
  - name: ansible.controller

  # Platform Integration (Optional)
  # - name: ansible.platform
  #   version: ">=1.0.0"

  # AAP Configuration as Code (Required for dynamic JT/WF creation)
  - name: infra.aap_configuration

  # Cryptography (Required for SSL certificate remediation playbook)
  # - name: community.crypto
  #   version: ">=2.0.0"
