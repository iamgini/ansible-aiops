# ============================================================================
# System Package Requirements for AIOps Execution Environment
# ============================================================================
#
# Format: package_name [platform:distribution]
# Bindep is used by ansible-builder to install system packages
#
# Compatible with: RHEL 9 based EE images
# Last Updated: 2026-07-10
# ============================================================================

# Git (required for cloning repositories in Lightspeed backend)
git [platform:rpm]

# Curl (required for Coder CLI installation and API calls)
curl [platform:rpm]

# OpenSSH Client (required for Coder SSH connections)
openssh-clients [platform:rpm]

# Python Development Headers (required for compiling Python packages)
python3-devel [platform:rpm]

# GCC Compiler (required for building Python extensions)
gcc [platform:rpm]
